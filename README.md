# DXCVendingMachine

A small vending machine app written in Angular.
Accepted coin denominations: `Euro (1, 2, 5, 10, 20, 50 cents, €1, €2)`.

## Build

Install dependencies:

```bash
npm install
```

Start local server:

```bash
ng serve
```

Navigate to: `http://localhost:4200/`

